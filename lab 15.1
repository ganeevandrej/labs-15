#include <iostream>
#include <cmath>

using namespace std;
float Power_A3(float A, float B) {
    B = A * A * A;
    cout << B << endl;
    return B;
 }
int main()
{
    int i, a[5];
    for (i = 0; i < 5; i++) 
        a[i] = rand() % 5;
    for (i = 0; i < 5; i++)
        a[i] =  Power_A3( a[i], 0);
    return 0;
}
