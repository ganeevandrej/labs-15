#include <iostream>
#include <cmath>

using namespace std;
int Sign(int x) {
    if (x > 0)
        x = 1;
    if (x == 0)
        x = 0;
    if (x < 0)
        x = -1;
    return x;
 }
int main()
{
    int A, B;
    cin >> A;
    cin >> B;
    cout << Sign(A) + Sign(B);
    return 0;
}
